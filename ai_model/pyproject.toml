[project]
name = "ai-chatbot-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "numpy",
    "tensorflow",
    "scikit-learn",
    "upstash-redis"
]

[tool.vercel]
entrypoint = "API:app"
